#ifndef LIBRARY_H
#define LIBRARY_H

#include <string>
#include <map>
#include <vector>

using std::string;
using std::vector;
using std::map;

const extern map<string, vector<string>> ExternFunctionsMap;

#endif
